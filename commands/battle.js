const db = require('../utils/database'); module.exports = { name: 'battle', execute: async (message, args) => { const target = message.mentions.users.first(); if (!target) return message.reply('Mention someone to battle!'); message.reply(`⚔️ Battle started with ${target.username}! Calculating winner...`); } };