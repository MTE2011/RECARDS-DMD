const db = require('../utils/database'); module.exports = { name: 'burn', execute: async (message, args) => { const id = args[0]; const inv = await db.getInventory(message.author.id); const idx = inv.cards.findIndex(c => c.instanceId === id); if (idx === -1) return message.reply('Card not found!'); inv.cards.splice(idx, 1); await db.saveInventory(inv); message.reply('🔥 Card burned! You gained 100 XP.'); } };