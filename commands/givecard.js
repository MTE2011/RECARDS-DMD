const db = require('../utils/database'); module.exports = { name: 'givecard', execute: async (message, args) => { const target = message.mentions.users.first(); const cid = args[1]; const inv = await db.getInventory(target.id); inv.cards.push({ instanceId: Date.now().toString(), cardId: cid, level: 1 }); await db.saveInventory(inv); message.reply('✅ Card given!'); } };