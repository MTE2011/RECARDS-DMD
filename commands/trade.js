const db = require('../utils/database'); module.exports = { name: 'trade', execute: async (message, args) => { const target = message.mentions.users.first(); const id = args[1]; if (!target || !id) return message.reply('Usage: .trade @user <instanceId>'); message.reply(`🤝 Proposed trade to ${target.username} for card \`${id}\`. (Accept with .accept)`); } };