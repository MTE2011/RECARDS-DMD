const db = require('../utils/database'); module.exports = { name: 'upgrade', execute: async (message, args) => { const id = args[0]; const inv = await db.getInventory(message.author.id); const card = inv.cards.find(c => c.instanceId === id); if (!card) return message.reply('Card not found!'); card.level++; await db.saveInventory(inv); message.reply(`🔥 Upgraded to Level ${card.level}!`); } };